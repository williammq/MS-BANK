server:
  port: 8090
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/williammq/MS-BANK
          search-paths: config-data
  application:
    name: Bank-MS
